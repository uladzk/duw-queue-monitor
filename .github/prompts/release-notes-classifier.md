Analyze the following commit log for a new release.

Classify the changes by reading each commit message carefully — do not rely only on
the commit type prefix. A `build` or `chore` commit may still describe a user-facing
change. Analyze the actual content of each message.

Generate release notes following your instructions.
Write the release notes in a user-friendly tone.
