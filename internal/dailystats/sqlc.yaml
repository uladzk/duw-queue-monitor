version: "2"
sql:
  - engine: "postgresql"
    queries: "queries.sql"
    schema: "../../db/migrations/"
    gen:
      go:
        package: "dailystats"
        out: "."
        sql_package: "database/sql"
        emit_json_tags: true
        emit_prepared_queries: false
        emit_interface: true
