version: "2"

linters:
  default: none
  enable:
    - govet
    - staticcheck
    - unused
    - ineffassign
    - misspell
    - gocyclo
  settings:
    gocyclo:
      min-complexity: 15

formatters:
  enable:
    - gofmt
